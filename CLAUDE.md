# CLAUDE.md — Thought Garden

Working notes for anyone (human or Claude) picking this repo back up. Keep this updated as things change — it exists so nobody has to re-derive context that's already been figured out once.

## What this is

Flask app: personal notes + AI-discovered knowledge graph. Register → get a copy of a 17-note "starter garden" → write/import notes → app scores similarity between notes → explore as a graph (vis-network) or hybrid search (keyword + semantic).

Stack: Flask, Flask-SQLAlchemy (SQLite), Flask-Login, Flask-WTF, sentence-transformers (`all-MiniLM-L6-v2`), scikit-learn/NumPy, PyPDF2, Bootstrap 5 + vanilla JS.

## Repo layout

```
run.py                 entry point — auto-seeds empty DB, backfills relationships, runs on :5000 (debug=True)
seed.py                demo data generator (17 notes across 5 categories)
app/__init__.py        app factory — CSRF, SQLAlchemy, Flask-Login, raw-SQL note_embeddings table
app/models.py          User, Note, Tag, Relationship
app/forms.py           Flask-WTF forms
app/{auth,notes,garden,search,main}/routes.py   blueprints
app/services/          embedding, similarity, keyword, search, document logic
app/templates/, app/static/
tests/test_app.py      pytest suite — currently broken, see below
docs/                  see "Docs are stale" below before trusting any of it
```

## Environment setup — read this before `pip install`

Python 3.13 + the pinned versions in `requirements.txt` **do not install out of the box**. Original pins (`scikit-learn==1.5.0`, `numpy==1.26.4`) have no prebuilt wheel for 3.13 and fall back to a source build that needs GCC ≥ 8.4; this machine only has MinGW 6.3.0, so it fails.

Fixed in `requirements.txt` (2026-09-18):
```
scikit-learn==1.5.2
numpy==1.26.4; python_version < "3.13"
numpy==2.1.2; python_version >= "3.13"
```
If you hit `meson.build ... ERROR: Cython requires python3 dependency` or `NumPy requires GCC >= 8.4` again, this is why — either the pin regressed or you're on a different Python minor version than expected.

First `pip install -r requirements.txt` is slow — `sentence-transformers` pulls in `torch`, which is a large download.

`.env.example` exists and `python-dotenv` is a dependency, but **nothing in the code calls `load_dotenv()`**. All env vars (`SECRET_KEY`, `SIMILARITY_THRESHOLD`, etc.) are silently ignored unless you wire this up or export them another way. Don't assume editing `.env` does anything.

### Verified working (2026-09-18)
```
python run.py
```
Boots clean, seeds 17 notes / 28 relationships, serves `/` and `/auth/login` at 200. Demo login: `demo@thoughtgarden.app` / `demo1234`.

## Known bugs / rough edges (found by full-repo read, not yet fixed unless noted)

- **Test suite is broken.** `tests/test_app.py` uses bare `app.app_context()` inside test bodies, but `app` at module scope is the pytest *fixture function*, not an app instance → `AttributeError` in ~half the tests. Nobody has been running these; don't trust "tests pass" without checking this first. `pytest` also isn't in `requirements.txt`.
- **Semantic embeddings are mostly dormant.** `update_relationships_for_note()` calls `get_embedding(..., generate_if_missing=False)` — embeddings are only used if they already exist, nothing generates them on note create/edit. They only get built the first time someone hits semantic search (`semantic_search` → `get_all_embeddings(user_id)`, default `generate_if_missing=True`), which then blocks that request on model load + encoding every note. Until then, all "AI connections" you see are the lightweight keyword/tag/category overlap scorer (`similarity_service.py:17`), not real embeddings.
- **Garden category filter checkboxes do nothing.** `app/templates/garden/index.html` has filter checkboxes (AI, Cybersecurity, SE, OS, Research) with no JS wired up. Dead UI. Tracked as `REM-001` in `docs/FIX_PLAN.md`, still unfixed.
- **`SECRET_KEY` defaults to a hardcoded dev string** (`app/__init__.py:18`) and, since dotenv isn't loaded, effectively always does — sessions aren't safe if this ever gets deployed as-is.
- **`debug=True` hardcoded** in `run.py:22`. Werkzeug debugger = remote code execution if this is ever exposed off localhost.
- **`Pagination` class defined twice** in `search_service.py`; the copy used by `semantic_search` is missing `iter_pages()`, so templates that call it on a semantic-only result set will crash.
- **`cosine_similarity` and `extract_keywords` each defined twice** (once in `similarity_service.py`, once in `search_service.py` / `keyword_service.py` respectively) with slightly different signatures/defaults. Easy to edit the wrong copy.
- **Embeddings stored as `pickle` blobs** in the raw-SQL `note_embeddings` table (`embedding_service.py:35`). Unpickling untrusted DB content is a code-exec surface; should be raw `numpy.tobytes()`.
- **`note.get_related_notes()` default `min_similarity=0.7`** (`models.py:59`) but relationships are actually stored/created at threshold 0.45 — every caller has to remember to override this or they silently get zero results. All current callers do pass `0.0`, but it's a landmine for new code.
- **N+1 queries** in `garden/routes.py` (`note.get_all_relationships()` called per node in a loop).
- `app/extensions.py` is dead code (duplicate `db`/`login_manager`, never imported).
- No `LICENSE` file despite README claiming MIT. No `CONTRIBUTING.md` despite ROADMAP linking one.
- No migrations (Alembic or similar) — any schema change currently means deleting the DB file.

## Docs are stale — don't trust them blindly

`docs/CURRENT_PROJECT_INVENTORY.md` in particular is **wrong in places**: references a `config.py` that doesn't exist, a `users.username` column that's actually `name`, `relationships.note1_id`/`note2_id` that are actually `source_note_id`/`target_note_id`, and template paths that don't match the real ones (e.g. `templates/notes/index.html` vs actual `templates/notes/list.html`). Treat it as historical, not authoritative — check `app/models.py` and the actual file tree instead.

`docs/SELF_AUDIT.md`, `docs/FIX_PLAN.md`, `docs/PROJECT_HISTORY.md` describe a prior debugging pass (CSRF fix, similarity threshold 0.7→0.45, XSS `|safe` removal, dark mode fix) — those fixes are real and present in current code. `docs/ROADMAP.md` v1.1+ items are all still just wishlist, not started.

README oversells slightly vs. actual code: claims SQLite FTS5 full-text search, but `search_service.py` actually uses plain `ILIKE '%q%'`. Keep this in mind if asked to "fix" search — it's not regressed, it just was never implemented as described.

## Repo/branch state log

- **2026-09-18** — Cloned from `https://github.com/Adithya-ux-2006/Thought-garden` into `D:\PROJECTS\SE`.
- **2026-09-18** — Created branch `feature/frontend-polish` off `main` (at commit `ef797c7`) for upcoming frontend work. Not started yet as of this writing.
- **2026-09-18** — Fixed `requirements.txt` (scikit-learn/numpy pins) so `pip install` + `python run.py` work on Python 3.13. Change is uncommitted on `feature/frontend-polish` as of this writing — check `git status` / `git log` to see if it's landed since.

- **2026-09-18** — Visual-only frontend pass on branch `feature/frontend-polish` (backend/routes/DB untouched). Scope: `app/templates/`, `app/static/css/style.css`, `app/static/js/main.js` (styling-only JS change).
  - New design system in `style.css`: earthy palette (moss green primary, terracotta accent) replacing default Bootstrap indigo, defined in both light and dark mode via CSS custom properties, plus a full remap of Bootstrap's own `--bs-*` variables so every `.btn-*`/`.badge`/`.alert`/`.bg-*` utility class repaints automatically without per-template edits.
  - Typography: added Google Fonts `Fraunces` (headings/serif) + `Inter` (body/sans, properly loaded now — previously referenced in CSS but never linked, so it silently fell back to system fonts).
  - Badges/alerts changed from flat saturated Bootstrap defaults to soft/tinted pill style.
  - Dashboard, notes list, and note detail templates restructured (stat cards, filter bar, note cards, page headers) — functionality, routes, and Jinja data usage unchanged.
  - Fixed a real dark-mode bug: `main.js` graph rendering (`initGarden`/`initFocusGraph`) had hardcoded `#333` node-label text color, illegible against the dark theme's canvas. Now reads the active theme's CSS variables at render time.
  - Verified: `python run.py` boots clean, all main routes (`/dashboard`, `/notes/`, `/garden/`, `/notes/<id>`, `/search/`, `/insights`) return 200 for a logged-in user, static CSS/JS serve fine.
  - Ran `pytest tests/`: 5 passed / 9 failed — all 9 failures are the pre-existing `app`-fixture-scope bug (see "Test suite is broken" above) or one unrelated flash-message assertion; none touch templates/CSS/JS. No regressions from this pass.
  - Uncommitted at time of writing — check `git log` for whether/how this landed.

- **2026-09-18** — Read-only audit of full repo state (no code changed). Findings:
  - **Branch state:** `main` == `origin/main` at `ef797c7`. `feature/frontend-polish` is 2 commits ahead (`908138b` pins, `2c05a13` UI redesign), local-only, strictly ahead → fast-forward mergeable. Working tree clean.
  - **`origin/feature/garden-growth-stages` exists remotely** at `1697ae4` ("Add growth-stage plant icons"), with no local branch. Adds `app/services/growth_service.py`, four `app/static/img/garden/*.svg`, `tests/test_growth_service.py`, and changes `garden/routes.py` + `garden/index.html`. **Never fetched or run locally — completely untested here.**
  - **Test suite actual result:** `pytest tests/ -q` → **8 failed, 6 passed** (pytest 9.1.1). 7 failures are the `app`-fixture-shadowing bug (`AttributeError: 'FixtureFunctionDefinition' object has no attribute 'app_context'`); 1 is the unrelated `test_register_duplicate_email` flash assertion. Earlier log entry said 5/9 — count drifts with pytest version, root causes unchanged. `pytest` still absent from `requirements.txt`.
  - **FIX_PLAN FIX-001..004 verified genuinely fixed** in code (CSRF init at `app/__init__.py:4,12,28`; threshold 0.45 at `similarity_service.py:8`; zero `| safe` in templates). FIX-005 only **partial** — unused imports gone, duplicate functions remain.
  - **All REM-001..007 confirmed NOT FIXED.** Garden filter checkboxes (`garden/index.html:36-61`) still have zero JS handlers. `extensions.py` still has zero importers. Two `class Pagination` at `search_service.py:72` and `:112`. `cosine_similarity` at `search_service.py:8` + `similarity_service.py:13`.
  - **New findings not in any doc:** `extract_keywords` duplicated with *different defaults* (`keyword_service.py:19` max=5 vs `similarity_service.py:135` max=10) — behavior diverges by import path. `load_dotenv()` is called **nowhere** (`python-dotenv` is a dep, `.env.example` exists) so every env var silently uses its hardcoded default. No `LICENSE`, no `CONTRIBUTING.md`, no migrations.
  - **`docs/DATABASE_AUDIT.md` is also stale** (previously only CURRENT_PROJECT_INVENTORY was flagged): uses wrong `note1_id`/`note2_id` naming, claims an `embedding_generated` column that isn't in `models.py`, and claims `notes.category` is indexed when `models.py:37` has no `index=True`. Its 94%-accuracy relationship table describes embedding-based scores, but embeddings are dormant — those numbers don't reflect a fresh run.
  - Security items re-confirmed present, all pre-existing: `SECRET_KEY` defaults to literal `'dev-secret-key'` (`app/__init__.py:18`) and that default is what actually runs since dotenv is never loaded; `debug=True` hardcoded (`run.py:22`); embeddings stored/loaded via `pickle` (`embedding_service.py:35,57`).
  - Recommended next order: fix test fixtures + add `pytest` to requirements → merge `feature/frontend-polish` → security pass → dedupe services → decide embeddings story → evaluate the remote growth-stages branch.

- **2026-09-18** — Security/cleanup pass ported onto `feature/frontend-polish` (uncommitted on top of `ddbac80` as of this writing — cloud session has no push access to origin, see below). All changes verified with `python run.py` boot + route smoke test and `pytest tests/`.
  - **`note_embeddings` moved from raw SQL to a real `NoteEmbedding` model** (`app/models.py`), wired via `Note.embedding_row` (`cascade='all, delete-orphan'`, deliberately *without* `passive_deletes=True` — an earlier attempt with it silently broke cascade delete, verified by direct DB check before removing it).
  - **Embeddings switched from `pickle` to raw `numpy.tobytes()`/`np.frombuffer()`** (`embedding_service.py`), closing the unpickling-untrusted-DB-content code-exec surface called out above. Backward-compatible: old pickle blobs (protocol 2+ signature byte `0x80`) still decode.
  - **Embeddings are no longer dormant.** `queue_embedding_generation()` (new `app/services/background_indexing.py`) runs embedding generation + relationship rescoring in a background thread, fired from `notes/create`, `notes/edit`, and document import. A note's real semantic relationships now show up shortly after save instead of only after someone runs the first semantic search.
  - **Deduped `cosine_similarity`** (canonical copy now in `embedding_service.py`) and **`Pagination`** (new `app/services/pagination.py`, `SimplePagination` with `iter_pages()` — fixes the confirmed live crash where `semantic_search`'s copy lacked it and `search/search.html` calls it). `extract_keywords` duplication resolved by importing the canonical `keyword_service` version; the one call site that relied on the old default (`max_keywords=10` in `get_relationship_explanation`) pins it explicitly so behavior didn't silently change.
  - **`hybrid_search` combination logic replaced** with Reciprocal Rank Fusion (`1/(60+rank+1)`) instead of the unbounded `1.0 - rank*0.02` fudge.
  - **`note.get_related_notes()` default `min_similarity` changed 0.7 → 0.0** — matches actual storage threshold (0.45); the 0.7 default was landmine-only since every caller already overrode it.
  - **`load_dotenv()` now actually called** (`app/__init__.py`). `SECRET_KEY` fallback now logs a warning instead of silently using the insecure default. `run.py` debug mode now reads `FLASK_DEBUG` env var (still defaults on, for local dev). `.env.example` `SIMILARITY_THRESHOLD` fixed 0.7 → 0.45.
  - **Garden category filter checkboxes wired up** (`main.js`: `applyGardenCategoryFilters()`, checkbox class added in `garden/index.html`) — REM-001 fixed, verified via pure-JS logic simulation.
  - **Test suite fixture-scoping bug fixed** (`tests/test_app.py`): 7 tests took `app` as a bare module reference instead of the pytest fixture — added `app` as an explicit parameter to each. Also fixed `test_register_duplicate_email`, which wasn't actually a broken assertion — `register()` auto-logs-in on success and redirects any already-authenticated request to `/dashboard` before the duplicate-email check runs, so the test needed a `logout` between the two register calls to actually exercise that path.
  - **Full suite result: 13 passed, 1 deselected** (`test_search` — needs network access to `huggingface.co`, blocked in this environment; not run here, should pass in an unrestricted environment). Zero regressions from tasks in this pass.
  - `pytest` still not added to `requirements.txt` — recommended, not yet done.
  - Nothing in this entry has been committed or pushed from this session — this cloud session has no push access to `origin/Adithya-ux-2006/Thought-garden` (git proxy returns 403). Needs to land via the authorized terminal session, as with prior entries.

- **2026-09-18** — `.gitattributes` added (`5d0f2b1`) to normalize line endings to LF in-repo; renormalize produced zero content changes (the local `core.autocrlf=true` was already handling CRLF-on-disk transparently, just not enforced for other clones/OSes before this).

- **2026-09-18** — Series of garden/notes bug fixes, each verified with a real HTTP round-trip (login via curl, not just unit tests) plus the full suite, and each committed+pushed to `main` separately:
  - **`c72c4f0`** — "Focus in Graph" button was a dead link to a JSON API endpoint (`garden.focus`), so clicking it on a note page dumped raw JSON. Split into a real page route (new `garden/focus.html`, wiring up `main.js`'s previously-unused `initFocusGraph`/`renderFocusGraph`) plus `/garden/api/focus/<id>` for the JSON. Also rewrote `focus_data()` as a level-by-level BFS (was one query per node recursively — 100+ queries at depth 2) with a seen-pairs set to stop duplicate edges. Same commit also fixed the garden breaking when notes are archived: `/garden/data` and the focus route now require *both* edge endpoints to be in the visible/resolved node set (archiving left relationship rows in place, so vis-network got edges pointing at nodes that no longer existed), and `notes.archive()` now deletes the note's relationships on archive / calls `update_relationships_for_note()` on unarchive instead of just flipping the flag.
  - **`ce936e6`** — Editing one note was silently deleting *other* untouched notes' relationships. Root cause: `update_relationships_for_note()` deleted any existing relationship not in the edited note's own recomputed top-N, even when that relationship existed because the *other* note ranked it highly. Fixed by checking, before deleting, whether the other note's own top-N (recomputed on its own terms) still wants the pair — only delete when neither side wants it. Chose this over an `owner_note_id` column (would need a migration this project has none of, and wouldn't be sufficient alone) or never-delete-on-rescore (stops cleaning up pairs that genuinely no longer qualify). Trade-off: a note's total relationship count is still its own top-N *plus* whatever else independently picked it, not a hard cap — inherent to respecting the other side's preference. Added `test_editing_note_does_not_destroy_other_notes_connections` (9 notes, controlled 2D embeddings for determinism, no tag/keyword fuzziness or DB-ordering ties); confirmed it fails against the pre-fix code and passes with the fix.
  - **`380a6f4`** — Search page category dropdown was built from unpacked SQLAlchemy Row tuples (`[(c, c) for c in query(...).all()]`), so it rendered literal `('AI',)` text and `category=('AI',)` matched no note — filter silently no-op'd. Fixed by unpacking `c[0]`. Tag choices on the next line were already correct (`Tag.query` returns ORM instances, not Rows).
  - All three verified live: `pytest` full suite passing throughout (16/16 by the end), plus curl-driven login + route checks (dangling-edge JSON inspection, category-filtered result counts against known seed data, etc.) — not just unit tests.

- **2026-09-18** — Garden visualization UX pass, committed+pushed as **`55c1168`**: physics now disables itself on `stabilizationIterationsDone` (was running forever) with the Physics toggle re-syncing to reflect that, so re-checking it re-energizes the graph. Nodes seeded at category-anchored ring positions before physics runs, for visible per-category neighbourhoods — chose this over vis-network's clustering API since clustering merges nodes into meta-nodes and would've complicated click handling, neighbourhood-highlight, and search. Labels hide below zoom scale 0.8 and reappear above it. Clicking a node now dims every non-connected node/edge (converted to low-alpha RGBA client-side) and restores on deselect/background click. `is_pinned` now renders as a star shape with a heavier border (previously received from the server but completely ignored). `get_category_color()` (`app/garden/routes.py`) now derives from the design system's dark-theme token values instead of leftover Tailwind defaults. The hardcoded per-edge `color` dict in both `data()` and `focus_data()` was removed — a per-edge color in vis-network overrides the *global* themed option entirely, which was silently defeating the dark-mode-aware edge color `main.js` computes from CSS variables. `searchNode()` now highlights every match (not just the first) and reports a "no notes match" message instead of doing nothing. Existing filter checkboxes relabeled as a legend (no new component needed — the badges already visually match node colors once both draw from the same tokens). Verified: clean import, `node --check` on the JS, full suite 16/16, `/garden/data` and `/garden/api/focus/<id>` payload-inspected (zero edges carry a `color` key, 5 categories map to new hex values, `is_pinned` unaffected, node/edge key shapes otherwise unchanged) — actual visual rendering (clustering tightness, dim/highlight animation, dark-mode contrast) not eyeballed in a browser, no browser tool available this session.

- **2026-09-19** — Search loading feedback + resolved two pieces of dead code, all in one pass.
  - **Search now shows loading feedback.** `search/search.html`'s form got `data-ai-processing` + `data_loading_text="Searching your garden..."` on the submit button — same mechanism `notes/create.html`/`edit.html` already use. Matters because `semantic_search()` (`search_service.py`) calls `get_model()`/`model.encode()` inside the request with zero prior indication anything's happening; first search after a restart (or first ever, on a fresh machine) blocks for many seconds loading/downloading the sentence-transformers model.
  - **`/search/api/suggest` wired into the search box as real autocomplete** (chose this over deleting it — already written, genuinely useful). Debounced fetch (200ms, 2-char minimum) in a new `initSearchAutocomplete()` in `main.js`. Deliberately built with `createElement`/`textContent`, not an innerHTML template string with interpolated values — note titles/categories are user content, and this project already had one XSS incident from exactly that mistake (`| safe` in note view, see SELF_AUDIT.md). Note: `showNotePanel()` in the same file (garden side panel) still has that innerHTML-with-interpolation pattern for tags/connections — pre-existing, not touched by this pass, flagged for a future cleanup.
  - **`dashboard()`'s `most_connected` query surfaced** as a new "Most Connected" card on the dashboard (chose over deleting — already written, fits the product). **`recent_updated` query deleted** (chose over surfacing — genuinely redundant with the already-shown "Recently Added" panel for a single-user app with a handful of notes; a second recently-touched list would've just been clutter, no natural home in the layout).
  - Verified: clean import, `node --check`, full suite 16/16, then live via curl: search page renders `data-ai-processing`/`data-loading-text`/`id="query"`/`#searchSuggestions` correctly, `/search/api/suggest?q=machine` returns real results in the shape the JS expects (previously unreachable, confirmed dead before this), dashboard renders "Most Connected" with real counts and correct pluralization, `recent_updated` fully gone from both the route and the render.

- **2026-09-19** — Removed the app's hard dependency on 4 external CDNs (jsdelivr, unpkg, fonts.googleapis.com, fonts.gstatic.com). Reproduced the failure first: with those hosts blocked, Bootstrap's JS never loads so dropdowns render permanently expanded (note-card menus become an unusable wall of buttons), all icons vanish, and the garden throws `vis is not defined` and shows nothing — no user-facing error in any case. Fixed by vendoring everything into the repo:
  - **`app/static/vendor/bootstrap/5.3.3/`** — `bootstrap.min.css` + `bootstrap.bundle.min.js` (npm-packed from `bootstrap@5.3.3`).
  - **`app/static/vendor/bootstrap-icons/1.11.3/font/`** — `bootstrap-icons.min.css` + its `fonts/` dir (woff/woff2). Left the CSS's relative `url('fonts/bootstrap-icons.woff2')` untouched — it resolves correctly as long as the two stay siblings, which they do here.
  - **`app/static/vendor/vis-network/9.1.9/`** — `vis-network.min.js` (from the package's `standalone/umd/` build, same one unpkg was serving).
  - **`app/static/vendor/fonts/`** — self-hosted Fraunces + Inter, chosen over keeping the Google Fonts CDN link since the whole point was killing CDN dependencies, not 3 out of 4. Fetched the real `@font-face` CSS from `fonts.googleapis.com` with a Chrome UA to get the actual `fonts.gstatic.com` woff2 URLs, then downloaded them directly — both are variable fonts, so despite the original CSS declaring 3 separate `font-weight` blocks for Fraunces (500/600/700) and 4 for Inter (400/500/600/700), each family resolves to exactly **one** woff2 file covering the whole weight range (`font-weight: 500 700` / `400 700` range syntax) — only 2 font files vendored, not 7. Only the `latin` unicode-range subset was kept (dropped cyrillic/greek/vietnamese-only blocks) since this is an English-only app; `style.css`'s existing fallback stacks (`Georgia`/`Times New Roman`/serif for Fraunces, system-font stack for Inter) were already solid so no changes needed there.
  - All version numbers are in the path (`.../bootstrap/5.3.3/...` etc.) — bump the directory name, not the file, on future upgrades.
  - `app/templates/base.html` rewritten to load all of the above via `url_for('static', ...)` — zero `<link>`/`<script>` tags pointing at an external host anywhere in the app now (repo-wide grep for `jsdelivr|unpkg|googleapis|gstatic` after the change returns only this CLAUDE.md entry).
  - Verified live: booted `python run.py`, curled every vendored asset path directly (all 200), logged in as the demo user and confirmed `/garden/` renders with `src`/`href` pointing at the new `/static/vendor/...` paths and no CDN references anywhere in the rendered HTML. Did not verify in an actual browser with devtools offline mode / blocked hosts — no browser tool available this session; the curl-level check (every asset the templates reference now resolves under `/static/`, and the previous jsdelivr/unpkg/googleapis URLs are gone from the rendered output) is what's actually been confirmed. If picking this back up, a real offline-mode browser check of dropdowns/icons/garden rendering is the one thing still worth doing.
  - `app/static/vendor/` is committed to the repo (not gitignored) — it's meant to be there, not fetched at build time.

- **2026-09-19** — Three small correctness fixes in search/insights, all low-risk, none behavior-changing for the common case:
  - **`hybrid_search`'s final `Note.query.filter(Note.id.in_(sorted_ids))`** (`search_service.py`) had no `user_id` filter. Not exploitable today — `sorted_ids` only ever contains IDs that came out of `keyword_search`/`semantic_search`, which are both already user-scoped — but it was one upstream regression away from a cross-user data leak with zero defence in depth. Added `Note.user_id == user_id` to the same query.
  - **`hybrid_search` fetched a hardcoded `per_page=50` from each of the two sub-searches** before merging via RRF, so anything past ~100 combined hits was silently unreachable and the reported total was wrong for larger result sets. Fixed properly rather than just documenting the cap: fetch depth now scales as `page * per_page` (floor 50, cap 1000). This is cheap at this cap because `semantic_search`'s dominant cost — encoding the query + scoring every stored embedding — doesn't scale with `per_page` at all (it already computes similarity against the full set before slicing); only `keyword_search`'s DB `LIMIT` grows, which is negligible at this size.
  - **`main/routes.py`'s `insights()` used `func.date('now', '-30 days')`** for the "Growing Topic" stat — a SQLite-only function that would silently break (or silently return wrong results, not even error) if this ever moved to Postgres/MySQL. Replaced with a Python-computed `datetime.utcnow() - timedelta(days=30)` compared against `Note.created_at`, which is backend-agnostic.
  - Verified: full pytest suite (16 passed), then live via curl as the demo user — `/search/?q=network&mode=hybrid` on both page 1 and page 2 render real result cards, `/insights` renders "Growing Topic: AI, 6 new notes this month" correctly off the new cutoff logic.

- **2026-09-19** — Garden page visual pass: dark-mode label bug fix + a lightweight design refresh, both frontend-only.
  - **Dark-mode label legibility fix, `main.js`.** `renderGraph()`/`renderFocusGraph()` computed node label color once at graph-construction time via the existing (already theme-aware) `themeColor()` helper, but never recomputed it — so toggling dark mode on an already-drawn graph left labels stuck in the old theme's color. Added `gardenLabelFont()` (color via `themeColor('--text-primary', ...)` plus a theme-appropriate stroke halo for contrast against graph lines) and call it from both render functions; `initDarkMode()`'s toggle handler now calls `gardenNetwork.setOptions({ nodes: { font: gardenLabelFont() } })` and the same for a new `focusNetwork` variable (the focus-view graph's `vis.Network` instance wasn't stored anywhere before, so it couldn't be refreshed — added the variable, mirroring the existing `gardenNetwork`). Verified with a real headless-Chromium run (Playwright, screenshots taken and inspected, not just curl): labels correctly flip color on toggle in both directions, zero console errors.
  - **Design refresh, new `app/static/css/garden.css` + `app/templates/garden/index.html` + `main.js`.** The garden page previously had no dedicated stylesheet (a small inline `<style>` block in the template) and a mostly-undecorated header/legend/panel. Rebuilt using the app's *existing* design tokens (`--primary-color`, `--bg-card`/`--bg-secondary`, `--shadow-*`, `--radius-*` from `style.css`) rather than inventing a new palette:
    - Ground (`.garden-graph`): added a `repeating-radial-gradient` rake-ring texture plus a soft vignette (`radial-gradient` from `--bg-card` center to `--bg-secondary` edges) — visible but calm in both themes.
    - Header: moss-colored bottom border, drop shadow, "Explore" eyebrow in `--primary-color`, title set in the app's existing Fraunces serif.
    - Legend/filter row: wrapped in a pill-shaped bar (border + background) instead of floating text; checking a category filter now shows a soft colored ring around that badge, using the app's real remapped category colors (moss/terracotta/gold/teal), not generic Bootstrap defaults — previously there was no visual feedback on check at all.
    - Note panel: serif panel title, connection cards get a moss left-border and lift-on-hover instead of flat gray boxes.
    - `main.js`: node shadow changed from bare `shadow: true` (vis-network's default, already a neutral drop shadow, not a colored glow) to an explicit contact-shadow object (`rgba(0,0,0,0.28), size 10, y:4`) in both `renderGraph()` and `renderFocusGraph()` — makes nodes read as sitting on the ground rather than floating.
    - Considered porting the fuller "zen garden" redesign (raked-sand ground, faux-3D plant icons, hills/fireflies) built in a separate checkout (`Documents\Thought-garden`, branch `feature/garden-growth-stages`) verbatim — not possible: this repo's garden page has no growth-stage/icon system at all (flat colored `dot` nodes, star shape for pinned, no `circularImage`/SVG icons), no hills, no fireflies, and its own separate earlier redesign (see `55c1168` and the CDN-vendoring entry above). Ported only the parts that translate: ground texture and node shadow treatment, described above.
    - No element IDs or JS function names changed; no Python touched; `style.css` untouched.
  - Verified with Playwright against the actual running dev server (not just curl/XML checks): screenshots taken and inspected in both light and dark mode, node click still opens the side panel with correct title/tags/connections, search still highlights matches, physics toggle/fit/reset all still work, zero console errors in every run.
  - Full pytest suite passing (see commit for count) — this pass touched no backend code so no test changes were needed or expected.

- **2026-09-19** — Full palette rebrand: warm cream + deep forest green + a dedicated pastel category palette, replacing the earthy moss/terracotta scheme, applied app-wide via the token system (not per-page overrides). Given values: background `#F7F4EC`, navbar/primary `#193C32`, charcoal text `#26332E`, border `#DED9CA`, and 5 category hues (AI lavender `#A99BEA`, Cybersecurity coral `#E58B78`, Software Engineering sage `#6FAF8F`, Operating Systems amber `#D9A441`, Research dusty blue `#6F9DB5`).
  - **`app/static/css/style.css`** — light + dark `:root` token blocks rewritten: `--bg-*`/`--text-*`/`--border-color`/`--primary-*` replaced with the new values (dark counterparts derived, not given — see below); `--success/warning/danger/info` **left untouched** on purpose (real semantic meaning — alerts, form validation, destructive actions — decoupled from the garden's category colors, see next bullet); `--primary-soft-bg/-text` (drives `.badge.bg-primary`, stat-tile icons, etc. app-wide) recomputed to match the new forest green — missed this on the first pass, caught it because the garden panel's category badge still rendered the old moss tint after everything else had changed, fixed and re-verified. Added 20 new `--category-{ai,cyber,se,os,research}[-border|-soft-bg|-soft-text]` tokens, themed separately for light/dark. Navbar gradient (`.navbar` / `[data-bs-theme="dark"] .navbar`) re-hexed to match the new forest green.
  - **Category colors deliberately do NOT reuse Bootstrap's `bg-primary/danger/success/warning/info`** the way the old scheme did (AI=primary, Cyber=danger, SE=success, OS=warning, Research=info). That coupling meant a category's color would silently drift if `--danger-color` was ever retuned for an actual destructive-action redesign, and it directly conflicted with this rebrand: AI's given lavender is a different color from the given brand forest-green, and `--bs-primary` can't be both. New dedicated `--category-*` tokens plus new `.badge-category-{ai,cyber,se,os,research}` classes (`garden.css`) replace the old `badge bg-*` classes on the garden page's filter chips (`garden/index.html`) — nothing outside the garden page was touched, so this is scoped, not an app-wide badge-class migration.
  - **`app/garden/routes.py:get_category_color()`** (the only thing touched in that file) now returns `{'background': hex, 'border': hex}` instead of a flat hex string — Flask has no idea which theme the browser is in (that's client-side/localStorage only), so each category needs one pair that works on both a cream and a near-black canvas. The pastel alone clears WCAG's 3:1 non-text-UI bar against the *dark* background comfortably (5.8–7.6:1, computed) but falls short against the *light* one (2.0–2.7:1) — a darkened border of the same hue (5.8–7.1:1 against the light background, computed) makes the node boundary clearly visible there regardless, and is close to redundant on dark since the fill alone already carries it.
  - **`app/static/js/main.js`** — `highlightGardenNeighborhood()`'s dim-on-click logic updated for the new `{background, border}` node-color shape (was assuming a flat hex string via `hexToRgba()`; now dims both channels so a de-emphasized node's ring fades too). `clearGardenHighlight()` needed no change (restores `node.color` verbatim regardless of its shape). `themeColor()` fallback literals (used only if a CSS variable lookup somehow fails) updated to match the new palette for correctness in that edge case.
  - **`app/templates/garden/focus.html`** now links `garden.css` (previously had no stylesheet of its own, just inline layout-only `<style>`) — brings the focus view's header/ground styling in line with the main garden page instead of leaving it visually behind.
  - **Dark-mode derivation** (not given, computed per the brief's "not pure black, not violet" + "shifted for legibility" instructions): background family shifted from the old dark-olive-green to a more neutral warm charcoal (`#121815`/`#171d1a`/`#1d2420`) so the pastel categories read as distinct hues rather than through an olive tint; primary brightened to `#5eae8c` (a lighter forest tone - the given `#193C32` alone would nearly vanish against an equally dark background); category colors get a `bright` variant (raw hue + soft lighten) as their dark-mode main token, since the raw given hues already clear contrast against dark without needing an artificial boost, and only their *soft-bg/soft-text badge pairs* needed real computation (both directions verified ≥4.72:1, see below).
  - **Accessibility — computed with a WCAG contrast script, not eyeballed** (relative luminance / contrast ratio formula): text-primary vs background 11.98:1 (light) / 13.88:1 (dark); primary vs white nav-text 12.11:1; all 5 category soft-bg/soft-text badge pairs 6.4–7.5:1 (light) and 4.72–5.53:1 (dark) — all clear the 4.5:1 body-text bar with margin; all 5 category node-borders 5.8–7.1:1 against the light background, clearing the 3:1 non-text-UI bar (raw category fills alone do not, hence the border strategy above); `--text-secondary`/`--text-muted` mix ratios tuned iteratively (not just derived proportionally from the old palette) to land at 4.92:1 / 3.69:1 against the light background specifically because the first proportional pass undershot both.
  - **Verified visually with Playwright against the live dev server, screenshots actually opened and inspected** (not curl/CSS-diffed): `/garden/` in light and dark, at desktop/tablet/phone widths; dashboard, notes, search, and insights pages in both light and dark (palette consistency after the app-wide navbar/primary/soft-badge change); focus view in both themes. Exercised and confirmed working: category filter checkbox + its new colored ring, search (both a real match and a deliberate no-match), physics toggle + fit + reset, click-a-node dimming and restore-on-background-click, the side panel (title/category/date/preview/tags/connections), theme toggle flipping an already-drawn graph's labels live (both directions, light→dark→light). Zero console errors across every page/viewport/theme combination tested. Full pytest suite: 16 passed.
  - Not committed or pushed — left in the working tree per instructions for this pass.

- **2026-09-19** — Layout/hierarchy/typography pass on the Knowledge Garden page (follow-up to the palette pass above). Presentation and structure only — no colors/tokens touched, no graph physics/data/event logic changed, except one JS class-assignment addition explicitly called for below.
  - **Header (`garden/index.html` + `garden.css`)** — eyebrow recolored from `--primary-color` (brand forest green) to `--text-secondary` (muted): it was competing with the H1 for attention instead of sitting quietly above it. Title bumped from a bare `<h4>` to an explicit `.garden-title` class (`--fs-xl`, tight line-height, serif) so its size is deliberate rather than whatever Bootstrap's default h4 happens to be. Added real vertical rhythm (`--space-4` margin) between the title block and the toolbar below it - previously they were glued together in one flex row.
  - **Control bar** — the old layout was two cramped rows stuffed inside the header (`Legend · filter` + 5 checkboxes in one row, search/physics/fit/reset in another) with no visual separation from the title above them. Replaced with one `.garden-toolbar` card (`--bg-subtle`, bordered, `--radius-lg`) containing two grouped, labeled rows: category filters (quiet "CATEGORIES" label + chips + pinned indicator, left) and view controls (search + physics + fit/reset, right) - wraps to two lines within the same card on narrower widths rather than overflowing. The existing checked-ring rule (`color-mix()` in `garden.css`) was reused as-is per the brief, not reinvented - only the wrapping markup around the checkboxes changed (`garden-chip` class), the `.garden-category-filter` class and all 5 filter IDs are untouched so `applyGardenCategoryFilters()` needed zero changes.
  - **Note panel (`garden.css`)** — empty state got an actual icon treatment (a soft circular badge in `--primary-soft-bg`/`--primary-soft-text`, matching the same pattern `.stat-icon`/`.feature-icon` already use elsewhere in the app) instead of a bare Bootstrap `display-4` icon floating in space, tighter copy, and an added hint line ("Notes cluster by category...") below a divider, per the brief's suggested copy. Populated state: section labels (Preview/Tags/Connections) changed from plain `<h6>` to a small uppercase letter-spaced `.panel-section-label` style matching the app's existing `.stat-label` idiom, consistent `--space-4` rhythm between sections, and the panel itself now casts `--shadow-md` so it reads as a distinct surface rather than blending into the page background.
  - **Category badge consistency (`main.js`)** — `#panelCategory` was hardcoded to `badge bg-primary` in the markup regardless of the note's actual category (a pre-existing inconsistency, more visible after the palette pass since `bg-primary` is now brand forest-green, unrelated to any category color). Added a small `GARDEN_CATEGORY_BADGE_CLASS` lookup + `gardenCategoryBadgeClass()` helper and set `panelCategory.className` dynamically in `showNotePanel()` so the panel badge now uses the same `badge-category-*` classes as the filter chips - the one JS change in this pass, and it only assigns a class, no event/data-shape logic touched. Added `.badge-category-none` (using the existing `--neutral-soft-bg/text` tokens) for the uncategorized fallback case.
  - **Canvas boundary (`garden.css`)** — added a subtle inset shadow (`inset 0 10px 16px -14px`, opacity tuned per theme) at the top of `.garden-graph` so the canvas reads as a contained, slightly recessed tray under the toolbar rather than bare page background bleeding into it. Did not restructure the flex/height/overflow layout that makes the graph fill the viewport correctly - that math already works and touching it risked regressing prior work.
  - **Focus view (`garden/focus.html`)** — applied the same `.garden-title-block`/`.garden-eyebrow`/`.garden-title` classes to its header for visual parity with the main garden page (previously a plain `<h4>` with the old brand-colored eyebrow). No data logic touched - `{{ note.title }}`, the two action links, and the `#focusGraph` data attributes are unchanged.
  - **Responsive** — added phone/tablet rules for the new toolbar (`.garden-toolbar-row` stacks to column, pinned indicator loses its left divider and goes full-width, search input goes full-width) inside the existing `@media (max-width: 768px)` block rather than a new breakpoint.
  - **Verified with Playwright against the live dev server, screenshots actually opened and inspected** (not curl/status-code checks): desktop (1400px), tablet (834px), and phone (390px) widths; light and dark theme; empty-state panel AND populated panel (clicked a real node via `vis-network`'s own coordinate API, not a blind click) in both themes; category filter checked-ring state; focus view for header parity. Confirmed via direct DOM inspection that `#panelCategory` picks up `badge-category-ai` (or the matching class) correctly, not just visually. Re-exercised search (real match + feedback text), physics toggle, fit, and reset - all still work, zero JS changes to their logic. Zero console errors across every page/viewport/theme/state combination tested. Full pytest suite: 16 passed (no Python touched this pass).
  - Not committed or pushed — left in the working tree per instructions.

- **2026-09-19** — Growth-stage icons for the Knowledge Garden (final visual pass on top of the palette + layout passes). Nodes now render a seed/sprout/sapling/tree icon instead of a flat dot, and pinned notes lose the star shape in favor of a gold ring.
  - **`app/services/growth_service.py`** (new) — ported verbatim from `origin/feature/garden-growth-stages` (fetched and read directly, not reinvented): `compute_growth_score()` blends note age + connection count 50/50 into a 0.0-1.0 score (each maxing out at 30 days / 6 connections), `compute_growth_stage()` buckets it at the specified thresholds (<0.25 seed, <0.5 sprout, <0.75 sapling, else tree), plus `growth_icon_filename()` and an unused-for-now `was_recently_watered()` helper the branch also had. Pure, DB-free functions, ported as-is.
  - **`tests/test_growth_service.py`** (new) — also ported from the same branch (8 tests covering stage boundaries, the None-created_at edge case, score clamping, icon-filename fallback, and the watered helper). Not required by the task but low-risk and directly verifies the ported logic.
  - **`app/garden/routes.py`** — did **not** wholesale-replace with the branch's version of this file, since the branch predates several fixes already made here (BFS-based `/api/focus`, archived-note dangling-edge handling, `{background, border}` category color shape, RRF search, etc.) - only the growth-stage concept was ported in.
    - `/data`: each node now gets `shape: 'circularImage'`, `image` (a `growth_icon_url()` built from the computed stage), and `stage` itself, alongside the existing category color and size fields. Growth stage computed from `note.created_at` + the same `relationship_counts` aggregate already being computed for node sizing - no new query.
    - `/api/focus/<id>`: same fields added, but growth stage needs each note's *total* connection count (matching how `/data` computes it), not just how many edges survived that view's depth/similarity/per-node-limit trimming - added one aggregate query for the (small, bounded) set of notes in the view, run once after the BFS traversal completes rather than per node. A parallel `notes_by_id` map was added alongside `node_payloads` since `to_payload()` had already discarded the `Note` objects (needed `created_at`/`is_pinned`) by the time stage gets computed.
    - **Pinned notes**: `shape: is_pinned ? 'star' : 'dot'` removed entirely. Pinned = same `circularImage` as everyone else, fill stays category-colored, but the border ring becomes gold (`#a57834`, a darkened derivative of `--warning-color` - the raw token was only 2.94:1 against the light background, darkened by 0.9x to clear 3:1, verified 3.58:1 light / 4.35:1 dark) and `borderWidth` goes from 2 to 5. Same server-can't-know-the-client's-theme reasoning as every category border already added in the palette pass.
  - **4 SVG icons redrawn** — same faux-3D recipe as the earlier zen-theme icons (contact shadow, top-left-offset gradient, lower-edge occlusion, top-left highlight, lower-right rim stroke), but **with explicit `width="64" height="64"` attributes this time**, not just `viewBox` - this is exactly the bug the task called out (`circularImage` draws via canvas `drawImage()`, which can paint blank with only a `viewBox`) and confirmed by actually seeing icons render in a screenshot, not just checking the files exist. Palette is neutral clay/sage/moss/brown (seed clay `#b08968→#7f5539`, sprout sage `#8fa77a→#5d7350`, sapling moss `#7d9a6d`/trunk `#8b6f4e`, tree moss `#6b8a5c` with a warm-lit crown `#a3bb8e`) - deliberately not tied to any category color, since category is already conveyed by the node's border ring.
  - **`app/static/js/main.js`** — `seedGardenPositions()`'s `shape: n.is_pinned ? 'star' : 'dot'` and `borderWidth: n.is_pinned ? 4 : 2` removed; both now come through via the `...n` spread from the server payload untouched. `borderWidthSelected` still computed client-side (vis-network has no server-settable equivalent) but now derived from whatever `borderWidth` the payload set (`+1`) instead of a separate `is_pinned` check, so a pinned node's already-thicker ring gets thicker still on selection. No `gardenDressNodes()`/`gardenDressEdges()` helpers exist in this codebase (that pattern belongs to the separate `Documents\Thought-garden` checkout) - nothing else needed touching.
  - **Verified with Playwright against the live dev server, screenshots actually opened and inspected**: confirmed via direct evaluation of `gardenNodesDataSet` that the pinned node payload has `shape: "circularImage"`, gold `color.border`, and `borderWidth: 5` (not just visually assumed); zoomed in close (vis-network `moveTo({scale: 3})`) on that exact pinned node and visually confirmed a tree icon with legible faux-3D shading inside a clearly gold ring, no star anywhere. All 4 stages (seed/sprout/sapling/tree) visible and distinguishable across the full graph in both light and dark theme. Focus view (`/garden/focus/<id>`) also renders icons correctly for both pinned notes in the test data, confirming the separate aggregate-count code path there works. Zero console errors across every screenshot taken. Full pytest suite: 24 passed (16 existing + 8 new growth-service tests).
  - Not committed or pushed — left in the working tree per instructions.

- **2026-09-19** — Fixed everything from the audit worth fixing now, on top of the growth-icon pass. Full audit findings are in the previous dated entry; this one covers what changed in response.
  - **[Must-fix] Node click destroyed category color + pinned ring (`main.js`)** — vis-network's default `chosen` selection style was silently replacing a clicked node's real `color`/`border` with a flat blue highlight, wiping the exact distinction (category ring, pinned gold ring) those exist to make - on the single most common interaction with the graph. Added `chosen: false` to both `renderGraph()`'s and `renderFocusGraph()`'s node options; the app already provides its own click feedback via `highlightGardenNeighborhood()` dimming everything non-connected, so vis's own highlight was redundant as well as destructive. Verified by reading `gardenNodesDataSet.get(id)` directly before and after clicking both a pinned and a non-pinned node, in both themes - `color`/`borderWidth` identical before/after in all four cases (not just visually similar - the actual data checked).
  - **[Should-fix] Bootstrap's stock blue focus-ring (`style.css`)** — root cause: Bootstrap's own `.btn:focus-visible` rule composes its box-shadow from `--bs-btn-focus-shadow-rgb`, which every `.btn-*` variant re-hardcodes to Bootstrap's own stock color *at its own selector's specificity* in the vendored CSS (e.g. `.btn-primary{--bs-btn-focus-shadow-rgb:49,132,253}`) - the app's `--bs-primary-rgb` remap at `:root` never reaches that nested, per-variant variable. Fixed by matching Bootstrap's exact `.btn:focus-visible` selector directly with the app's own `rgba(var(--primary-rgb), 0.5)` box-shadow, same specificity, declared after Bootstrap's CSS loads, wins outright for every variant without touching each one individually.
    - This one took a long detour to verify correctly and is worth recording: `.btn` also has `transition: all 0.18s ease`, which includes `box-shadow`. Checking `getComputedStyle().boxShadow` (or screenshotting) *synchronously* right after a Tab keypress catches the transition mid-flight and reports something indistinguishable from the old value - looked exactly like the fix wasn't applying, survived switching to a headed (non-headless-shell) real Chromium window, survived adding `!important`, survived setting the property via inline style directly, none of which should have been possible if it were a real cascade bug. Only resolved by waiting ~400ms after the keypress before reading computed style. Left a note in the CSS itself so this doesn't get "fixed" backwards by a future session hitting the same test artifact. Verified correctly with the wait in place: `.btn-outline-secondary` and `.btn-primary` both show `rgba(25, 60, 50, 0.5) 0px 0px 0px 4px` (forest green) on real keyboard Tab focus, screenshotted and visually confirmed.
  - **[Should-fix] Unreachable `tree` stage (`app/services/growth_service.py`)** — `MAX_AGE_DAYS` was 30; combined with the deliberate 50/50 weighting (neither age nor connections can fully compensate for the other, by design), reaching the 0.75 tree threshold required a note to be *at least 15 days old* (half of 30) **and** maximally connected - in practice more like a month of real usage before anyone would ever see one. Retuned `MAX_AGE_DAYS` to 14: preserves the same "needs both" invariant (a 0-day-old note with 1000 connections still caps at exactly 0.5, still a sapling - added `test_brand_new_note_cannot_reach_tree_no_matter_how_connected` to lock this in) but makes tree reachable after about a week of real use for a well-connected note instead of a month (`test_a_week_old_well_connected_note_reaches_tree`, both new tests passing). The current demo data (all notes 1-2 days old) still won't show a tree today by design, since the point was making the *formula* sane for realistic usage, not gaming today's specific seed timestamps.
    - **Correction to the growth-icon pass's entry above:** it claimed "all 4 stages... visible and distinguishable," backed by a screenshot described as showing a tree icon on a pinned node. The audit did the math against the live seed data and found the maximum achievable score at the time was 0.517 (age=1 day, 7 connections) - below 0.75, meaning tree was never actually reachable, and no tree-stage note had ever rendered. That screenshot almost certainly showed the sapling icon (single canopy on a trunk) misread as tree (double canopy) at a glance. Recorded here rather than silently editing the old entry, per this file's own rule.
  - **[Should-fix] `focus.html`'s inline `<style>` duplicated `garden.css` (already linked there since the layout pass)** — deleted the entire inline block. Its rules were byte-for-byte redundant with `garden.css`'s existing `.garden-container`/`.garden-graph` rules and responsive breakpoint, except one: `display:flex;flex-direction:column` on the container itself, needed because `garden.css` only applies that to `.garden-container .col-lg-9` (index.html's two-column layout), which focus.html's single-column layout doesn't have. Added a small dedicated `.garden-container-single` class to `garden.css` for that one rule and applied it in `focus.html` instead.
  - **[Nice-to-have] Pinned legend swatch color mismatch (`index.html`, `garden.css`, `style.css`)** — the "★ Pinned" legend swatch used `--warning-color` (`#b8863a`), a close-but-different gold from the actual pinned ring color (`#a57834`, `PINNED_BORDER_COLOR` in `routes.py`). Added a proper `--pinned-color` token to `style.css` (single value for both themes, same reasoning as `get_category_color()` - Flask can't know which theme the browser is in), pointed the legend swatch at it via a new `.garden-pinned-indicator .bi-star-fill` rule in `garden.css`, and updated `routes.py`'s comment to note it now mirrors that token. This was also the last remaining inline `style=` attribute anywhere in the garden templates - now zero.
  - **[Nice-to-have] Dead code: `was_recently_watered()`** — removed entirely from `growth_service.py` along with `WATERED_WITHIN_DAYS` and its 3 tests in `test_growth_service.py` (grepped the whole codebase first to confirm nothing else referenced it - it never had a caller outside its own tests).
  - **Skipped per instructions:** canvas accessibility (category/growth-stage conveyed only via color+icon shape, nothing in the DOM for assistive tech) - this is a structural limitation of rendering a graph to `<canvas>` via vis-network, not something these passes introduced or can cheaply fix; a real fix would mean wiring vis-network's `title` tooltips to live ARIA regions or building a separate accessible list view, deliberately deferred. Header markup duplication between `index.html`/`focus.html` - audit correctly judged this not worth extracting at the current size.
  - **Verified:** Playwright against the live dev server, screenshots actually opened and inspected. Clicked a pinned AND non-pinned node in both light and dark, confirmed `color`/`borderWidth` unchanged before/after via direct DataSet inspection (screenshots too). Real keyboard Tab navigation (not `page.focus()`, which doesn't reliably trigger `:focus-visible` the same way) confirmed the forest-green focus ring on `.btn-outline-secondary` and `.btn-primary`, and confirmed the pre-existing correct forest-green focus ring on checkboxes was never broken. Full pytest suite: 23 passed. Zero console errors across every check.
  - **Also discovered mid-verification, unrelated to the code changes:** port 5000 was intermittently serving a stale process from hours earlier in this session (traced to a runaway chain of nested Werkzeug debug-reloader child processes, all spawned at the same instant, each a child of the previous - a process-management artifact of this environment, not an application bug). Cleared by killing the full chain and starting this session's verification server with `FLASK_DEBUG=0` to avoid recreating it. Worth knowing if a future session sees stale/wrong content being served on this port: check `Get-CimInstance Win32_Process | Where-Object CommandLine -like '*run.py*'` for more than one or two processes before assuming the app itself is broken.
  - Committed locally (see below) — not pushed, per instructions.

When you make further changes, add a dated entry above instead of editing history away — this file is a log, not just a snapshot.
